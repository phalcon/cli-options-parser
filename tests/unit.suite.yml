# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    # enabled modules and helpers
    enabled:
        - Asserts
        - Helper\Unit
    config:
